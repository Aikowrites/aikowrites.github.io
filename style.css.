/* General Styles */
body {
    font-family: Arial, sans-serif;
    background-color: #121212; /* Dark background */
    color: #f0f0f0; /* Light text */
    margin: 0;
    padding: 0;
}

/* Header */
header {
    text-align: center;
    padding: 20px;
    background: linear-gradient(45deg, #0f0c29, #302b63, #24243e); /* Neon gradient */
    color: #f0f0f0;
}

header h1 {
    font-size: 2.5em;
}

header p {
    font-size: 1.2em;
    margin-top: 5px;
}

/* Sections */
section {
    padding: 20px;
    margin: 20px auto;
    max-width: 800px;
    background: #1e1e1e; /* Dark gray section background */
    border-radius: 10px;
    box-shadow: 0 4px 6px rgba(0, 0, 0, 0.5);
}

section h2 {
    color: #ff4b5c; /* Neon pink */
    font-size: 1.8em;
    border-bottom: 2px solid #ff4b5c;
    padding-bottom: 5px;
    margin-bottom: 10px;
}

/* Links */
a {
    color: #08fdd8; /* Neon teal */
    text-decoration: none;
}

a:hover {
    text-decoration: underline;
}

/* Footer */
footer {
    text-align: center;
    padding: 10px;
    background: #24243e;
    color: #fff;
    font-size: 0.9em;
}
